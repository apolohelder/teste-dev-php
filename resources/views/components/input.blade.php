<div class="form-floating mb-3">
    <input type="{{ $typeinput }}" class="form-control" id="{{ $infor }}" name="{{ $infor }}"
        value="{{ $value }}" placeholder="{{ $placeholder }}">
    <label for="{{ $infor }}">{{ $text }}:</label>
</div>
